const links = {
  display: "flex",
  flexDirection: "row",
  columnGap: 1,
  alignItems: "center",
  justifyContent: "space-between",
  cursor: "pointer",
}

const toolbar = {
  width: "100%",
  display: "flex",
  flexDirection: "row",
  columnGap: 1,
  alignItems: "center",
  justifyContent: "space-between",
}

const footerToolbar = {
  width: "100%",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "space-between",
}
export { links, toolbar, footerToolbar }
